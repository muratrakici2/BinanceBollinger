import React from 'react'

const Getsymbol = () => {
    // Güncel Sembol Çekme
  //   axios.get('https://api.binance.com/api/v3/exchangeInfo')
  //   .then(function (response) {
  //     // handle success
  //     const sembol = response.data.symbols;
  //     const semboller = sembol.map((i)=>i.symbol)
  //     console.log(semboller)
  //     const filteredUser = semboller.filter((data) => {
  //       return data.toLowerCase().indexOf("usdt".toLowerCase()) !== -1});
  //     console.log(filteredUser)
  return (
    <div>Getsymbol</div>
  )
}

export default Getsymbol